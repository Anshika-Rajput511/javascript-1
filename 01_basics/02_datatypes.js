"use strict"; // treat all js code as newer version

// alert("hello") //can't use here because we are using node.js not browser
// used on console because js engine is inside browser

let name = "Anshika" //string
let age= 20; //number
let isHonest = true; //boolean
let fine = null;

//number => 2 to power 53
//bigint
//string => ""
//boolean => true/false
//null is a standalone value 
//undefined is when we do not initialize a value in variable
//symbol is used for uniqueness

//object 

console.log(typeof age)  //number
console.log(typeof null)   //object
console.log(typeof undefined)   //undefined
